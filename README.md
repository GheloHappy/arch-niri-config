#Arch Niri Noctalia

## Requirements

##Directory must be on ~/arch-niri-config

### OS: CachyOS (No Desktop)

### Setups initialization
- sudo pacman -Syu
- sudo pacman -S --needed git base-devel

cd ~

git clone https://github.com/GheloHappy/arch-niri-config.git

sudo chmod +x arch-niri-config/install.sh

cd arch-niri-config

sudo ./install.sh

---

###Copy of my default setups is in scripts/
